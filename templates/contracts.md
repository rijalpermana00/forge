# Frontend Contract Inventory

> Generated by `forge scan --contracts` on {{generatedAt}}. This is a **factual
> extraction** of every API call and data shape found in this codebase — the
> contract surface a backend must satisfy. Deterministic, no interpretation.
> Paste it into your AI tool as grounding when prompting for the backend
> (e.g. `forge blueprint <feature> --mode backend --from specs/CONTRACTS.md`).
> Re-run `forge scan --contracts` to refresh.

## Project

{{project}}

## Consumed endpoints ({{endpointCount}})

Every HTTP call found in source (axios-style `.get/.post/...` and `fetch`).
`:param` marks a dynamic path segment. Response type is the TypeScript generic
at the call site, when present.

{{endpoints}}

## Data shapes ({{shapeCount}})

Exported interfaces, type aliases, enums, and Zod schemas — the request/response
payloads and domain models the frontend expects.

{{shapes}}
